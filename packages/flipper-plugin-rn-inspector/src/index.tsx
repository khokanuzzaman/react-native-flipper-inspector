import React from 'react';
import ReactDOM from 'react-dom/client';
import { App } from './App';

// Plugin component
const Plugin: React.FC = () => {
  return <App />;
};

// Export the plugin
export { Plugin };
